apiVersion: apps/v1
kind: Deployment
metadata:
  name: webserver-deployment
  namespace: argoweb
  labels:
    app: webserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webserver
  template:
    metadata:
      labels:
        app: webserver
    spec:
      containers:
      - name: webserver
        image: deepanshu1411/alpine-webserver:67
        ports:
        - containerPort: 80
      imagePullSecrets:
      - name: regcred
